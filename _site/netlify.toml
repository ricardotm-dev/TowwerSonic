[build]
  # Forzamos la instalación de gemas antes de construir
  command = "bundle install && bundle exec jekyll build"
  publish = "_site"

[build.environment]
  # Usamos una versión moderna pero estable de Ruby
  RUBY_VERSION = "3.3.0"
  # Evitamos errores de encoding
  LC_ALL = "C.UTF-8"
